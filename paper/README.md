# Paper Experiments

Reproducible experiments for the SIAM/DS paper on polynomial-based parameter estimation.

## Prerequisites

You need the following installed before proceeding:

- **Julia 1.10+**
- **Globtim** — clone from the shared repo into a sibling directory
- **GlobtimPostProcessing** — clone from the shared repo into a sibling directory

Your directory layout must look like:

```
parent_dir/
  DynamicObjectives/     # this repo
  globtim/                # Globtim package
  globtimpostprocessing/  # GlobtimPostProcessing package
```

## Setup

```bash
# 1. Clone all three repos into the same parent directory
cd parent_dir
git clone git@github.com:gescholt/DynamicObjectives.jl.git
git clone <globtim-repo-url> globtim
git clone <globtimpostprocessing-repo-url> globtimpostprocessing

# 2. Run setup (adds Globtim + GlobtimPostProcessing as dev dependencies)
cd DynamicObjectives.jl
julia setup_dev_packages.jl
```

The setup script will error if the sibling directories are not found.

## Running experiments

### From the monorepo root (recommended)

```bash
# Run a single experiment
julia --project=. experiments/drivers/run_experiment.jl DynamicObjectives/paper/configs/lv2d.toml

# Run all 16 paper experiments
julia --project=. experiments/drivers/run_experiment.jl DynamicObjectives/paper/configs/*.toml

# Validate configs without executing (dry run)
julia --project=. experiments/drivers/run_experiment.jl --dry-run DynamicObjectives/paper/configs/*.toml
```

### From the DynamicObjectives directory

```bash
cd DynamicObjectives
julia --project=. paper/run_experiment.jl paper/configs/deuflhard_2d.toml
julia --project=. paper/run_experiment.jl paper/configs/*.toml
```

Results are written to the `[output].dir` path in each TOML config.

## Post-processing saved results

After running an experiment, re-run the analysis without re-computing:

```bash
# From monorepo root
julia --project=. experiments/drivers/postprocess_experiment.jl results/lv2d/ --all

# From DynamicObjectives/
julia --project=. paper/postprocess_experiment.jl results/lv2d/ --all
```

## Experiment list

| Config | Section | Description | Type |
|--------|---------|-------------|------|
| `lv2d.toml` | 4.1 | Lotka-Volterra 2D, globally identifiable | ODE |
| `lv3d.toml` | 4.2 | Lotka-Volterra 3D, partial observability | ODE |
| `lv4d_eps.toml` | 4.3 | Constrained LV4D, eps-parameterized | ODE |
| `lv4d_simple.toml` | 4.3 | LV-4D simple, non-identifiable (negative result) | ODE |
| `fhn3d.toml` | 4.4 | FitzHugh-Nagumo 3D | ODE |
| `goodwin4d.toml` | 4.5 | Goodwin oscillator 4D, Hill function | ODE |
| `lv2d_time_T1.toml` | 5.2 | Time sensitivity, T=1 | ODE |
| `lv2d_time_T10.toml` | 5.2 | Time sensitivity, T=10 | ODE |
| `lv2d_time_T100.toml` | 5.2 | Time sensitivity, T=100 | ODE |
| `daisy4d.toml` | 5.4 | DAISY Ex3 4D compartmental model | ODE |
| `locally_id_1d.toml` | 5.9 | 1D locally-identifiable | ODE |
| `locally_id_2d.toml` | 5.9 | 2D locally-identifiable | ODE |
| `deuflhard_2d.toml` | benchmark | Deuflhard 2D polynomial | analytical |
| `ackley_3d.toml` | benchmark | Ackley 3D multi-modal | analytical |
| `levy_3d.toml` | benchmark | Levy 3D multi-modal | analytical |
| `griewank_3d.toml` | benchmark | Griewank 3D multi-modal | analytical |

Analytical benchmarks run faster (no ODE solves). Start with `deuflhard_2d.toml` to verify the pipeline works.

## Directory structure

```
paper/
  configs/           16 experiment TOML configs
  catalogue/         Pre-built model catalogue JSONL files (10 files)
  register_models.jl         Regenerate base catalogue files (covers 7 of 42 entries; most are auto-generated by screening)
  manifest.toml              Experiment manifest
```

## Catalogue files

ODE experiments require catalogue JSONL files that define the model function,
true parameters, initial conditions, and bounds. These are pre-built and shipped
in `catalogue/`. No action required.

To regenerate base entries (not normally needed; only covers 7 of 42 entries,
the rest are auto-generated by screening scripts):

```bash
julia --project=. paper/register_models.jl
```
